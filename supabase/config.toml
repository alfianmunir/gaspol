# Supabase CLI config for the Gaspol backend.
# Link to the shared project:  supabase link --project-ref ticdiatbdxkmpzmqvntn
project_id = "ticdiatbdxkmpzmqvntn"

[functions.fit-food-estimate]
# Signed-in users only; the function also checks the premium flag.
verify_jwt = true

[functions.fit-weekly-review]
# Invoked by the scheduler with x-cron-secret, not a user JWT.
verify_jwt = false
