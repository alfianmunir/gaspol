# Local environment values (never commit)
# NOTE: config.js is committed on purpose — it holds only the public
# anon/publishable key (RLS-protected), never the service-role key.
.env
supabase/.env

# Dependencies / tooling
node_modules/
.DS_Store
